# Homologação (HML) example env file
# Copy to .env.hml and adjust values.

# --- Postgres (HML) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me

# --- Backend (Spring) ---
SPRING_PROFILES_ACTIVE=hml
RULEX_SECURITY_ENABLED=true

# Basic Auth users (HML)
RULEX_ADMIN_USERNAME=admin
RULEX_ADMIN_PASSWORD=change-me
RULEX_ANALYST_USERNAME=analyst
RULEX_ANALYST_PASSWORD=change-me

# CORS allowed origins (CSV). Set to your HML frontend URL(s).
# Example: https://rulex-hml.example.com,https://rulex-hml2.example.com
RULEX_CORS_ALLOWED_ORIGINS=http://localhost:5173

# --- Frontend (Vite) ---
# Used by the frontend to authenticate against the backend (HTTP Basic)
VITE_API_BASIC_AUTH=admin:change-me

# Optional: override if your backend is not reachable via the Vite proxy
# VITE_JAVA_API_URL=
# VITE_OAUTH_PORTAL_URL=
# VITE_APP_ID=
