# Gaps Register

## Open
- Backend tests fail to compile locally due to missing `ComplexRuleEvaluator` symbol in multiple test classes.

## Closed
- E2E navigation selectors referenced a legacy label ("Regras de Fraude"). Updated to current UI label ("Regras Complexas").
