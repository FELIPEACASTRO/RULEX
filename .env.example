# Copie para .env e ajuste conforme necessário.
# Nunca commite .env com credenciais reais.
#
# ⚠️ IMPORTANTE: Todas as variáveis abaixo são OBRIGATÓRIAS.
# O docker-compose falhará se não estiverem definidas.
#
# 🔧 DESENVOLVIMENTO LOCAL: As senhas abaixo funcionam para dev/test.
# 🚨 PRODUÇÃO: Use senhas FORTES (mínimo 12 caracteres, letras, números e símbolos).

# ============================================
# DATABASE - PostgreSQL
# ============================================
POSTGRES_USER=postgres
# DEV: senha simples para desenvolvimento local
# PROD: Use senha forte! Exemplo: POSTGRES_PASSWORD=MyStr0ng!P@ssw0rd2024
POSTGRES_PASSWORD=postgres

# ============================================
# DATABASE - Neo4j Graph
# ============================================
# DEV: senha simples para desenvolvimento local
# PROD: Use senha forte! Exemplo: NEO4J_PASSWORD=MyStr0ng!N3o4j2024
NEO4J_PASSWORD=neo4jpassword

# ============================================
# BACKEND - Spring Profile
# ============================================
# Opções: dev, staging, prod
SPRING_PROFILES_ACTIVE=dev

# ============================================
# RULEX Security (HTTP Basic Auth)
# ============================================
RULEX_SECURITY_ENABLED=true

# Admin user (full access)
RULEX_ADMIN_USERNAME=admin
# DEV: senha simples para desenvolvimento local
# PROD: Use senha forte! Mínimo 12 chars, maiúsculas, minúsculas, números e símbolos
RULEX_ADMIN_PASSWORD=rulex

# Analyst user (read-only + analyze)
RULEX_ANALYST_USERNAME=analyst
# DEV: senha simples para desenvolvimento local
RULEX_ANALYST_PASSWORD=rulex

# ============================================
# FRONTEND - API Authentication
# ============================================
# Formato: usuario:senha (deve corresponder a um usuário acima)
VITE_API_BASIC_AUTH=admin:rulex
